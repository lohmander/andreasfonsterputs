apiVersion: apps/v1
kind: Deployment
metadata:
  name: hemforbattringen-wagtail
  labels:
    app: hemforbattringen-wagtail
spec:
  replicas: 1
  selector:
    matchLabels:
      app: hemforbattringen-wagtail
  template:
    metadata:
      labels:
        app: hemforbattringen-wagtail
    spec:
      imagePullSecrets:
        - name: regcred
      initContainers:
        - name: migrate
          image: docker.pkg.github.com/aldowntown/hemforbattringen/wagtail-hemforbattringen:v1.0.0-alpha.2
          command: ["python", "manage.py", "migrate"]
          envFrom:
            - secretRef:
                name: secret-config
        - name: collectstatic
          image: docker.pkg.github.com/aldowntown/hemforbattringen/wagtail-hemforbattringen:v1.0.0-alpha.2
          command: ["python", "manage.py", "collectstatic", "--noinput"]
          envFrom:
            - secretRef:
                name: secret-config
      containers:
        - name: wagtail
          image: docker.pkg.github.com/aldowntown/hemforbattringen/wagtail-hemforbattringen:v1.0.0-alpha.2
          envFrom:
            - secretRef:
                name: secret-config
          ports:
            - containerPort: 8000
---
apiVersion: v1
kind: Service
metadata:
  name: hemforbattringen-wagtail
spec:
  selector:
    app: hemforbattringen-wagtail
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: hemforbattringen-nextjs
  labels:
    app: hemforbattringen-nextjs
spec:
  replicas: 1
  selector:
    matchLabels:
      app: hemforbattringen-nextjs
  template:
    metadata:
      labels:
        app: hemforbattringen-nextjs
    spec:
      imagePullSecrets:
        - name: regcred
      containers:
        - name: nextjs
          image: docker.pkg.github.com/aldowntown/hemforbattringen/nextjs-hemforbattringen:v1.0.0-alpha.1
          envFrom:
            - secretRef:
                name: secret-config
          env:
            - name: GRANIT_WAGTAIL_API_URL
              value: http://hemforbattringen-wagtail.hemforbattringen-production.svc.cluster.local/api/wagtail/v2/
          ports:
            - containerPort: 3000
---
apiVersion: v1
kind: Service
metadata:
  name: hemforbattringen-nextjs
spec:
  selector:
    app: hemforbattringen-nextjs
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
---
apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: hemforbattringen
  annotations:
    cert-manager.io/cluster-issuer: selfsigned-issuer
spec:
  tls:
    - hosts:
        - hemforbattringen.mygranit.site
      secretName: tls-proxied-self-signed
  rules:
    - host: hemforbattringen.mygranit.site
      http:
        paths:
          - path: /admin
            backend:
              serviceName: hemforbattringen-wagtail
              servicePort: 80
          - path: /api/preview
            backend:
              serviceName: hemforbattringen-nextjs
              servicePort: 80
          - path: /api
            backend:
              serviceName: hemforbattringen-wagtail
              servicePort: 80
          - path: /
            backend:
              serviceName: hemforbattringen-nextjs
              servicePort: 80
